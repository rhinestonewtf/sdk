---
"@rhinestone/sdk": minor
---

Add per-chain session configuration for smart sessions, replacing the single-session-only API. Sessions can now be configured differently per chain via the `sessions` map, and `verifyExecutions` is no longer exposed in the public API.
