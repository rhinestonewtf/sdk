---
"@rhinestone/sdk": patch
---

Default orchestrator requests to the `2026-04.blanc` API version and translate CAIP-2 chain ids at the HTTP boundary so the SDK keeps exposing numeric chain ids to callers.
