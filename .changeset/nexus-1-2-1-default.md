---
"@rhinestone/sdk": minor
---

Nexus now defaults to v1.2.1. This changes the default counterfactual account address; opt back into the previous implementation with `account: { type: 'nexus', version: '1.2.0' }`.
