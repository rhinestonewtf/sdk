{
  "private": true,
  "scripts": {
    "build": "bun run clean && tsc && tsc-esm-fix --target=src/dist",
    "clean": "rimraf src/dist",
    "test": "vitest",
    "test:integration": "vitest --config vitest.config.integration.ts",
    "test:integration:smoke": "vitest --config vitest.config.integration.ts test/integration/scenarios/smoke.itest.ts",
    "test:integration:smoke:debug": "SDK_ITEST_DEBUG=1 vitest --config vitest.config.integration.ts test/integration/scenarios/smoke.itest.ts",
    "check": "biome check",
    "typecheck": "tsc --noEmit",
    "test:types": "tsc --noEmit -p tsconfig.type-tests.json",
    "reference:extract": "typedoc --options scripts/reference/typedoc.json",
    "reference:generate": "bun run scripts/reference/generate.ts",
    "reference": "bun run reference:extract && bun run reference:generate",
    "changeset": "changeset",
    "changeset:release": "bun run build && changeset publish",
    "changeset:version": "changeset version && bun run scripts/sync-version.ts",
    "size": "size-limit"
  },
  "workspaces": [
    "./src"
  ],
  "dependencies": {
    "@rhinestone/shared-configs": "^1.7.0",
    "viem": "^2.40.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.0",
    "@changesets/cli": "^2.28.1",
    "@size-limit/esbuild-why": "^11.2.0",
    "@size-limit/preset-small-lib": "^11.2.0",
    "@types/bun": "^1.2.8",
    "jose": "^6.2.2",
    "rimraf": "^6.0.1",
    "size-limit": "^11.2.0",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.13",
    "tsc-esm-fix": "^3.1.2",
    "typedoc": "^0.28.19",
    "typescript": "^5.8.2",
    "vitest": "^3.1.2"
  }
}
